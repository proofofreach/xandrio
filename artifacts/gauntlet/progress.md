# Xandrio product Gauntlet

Updated: 2026-08-21

Status: **WS-00 passed at 94/100; WS-01 is in progress (baseline 79/100; first repaired slice 98/100)**

## Quality contract

- Qualitative gate: fresh critic score at least 90/100, competitive or preferable for Xandrio's use case, with no critical/high defect.
- External gate: repository tests, real-browser PWA smoke, calibrated audio policy, playback timing, player layout, dependency audit, and Docker-context checks.
- Evidence rule: critics operate the running product. Builder summaries and code inspection alone cannot pass a workstream.
- Safety rule: use synthetic or public fixtures only. Never expose private library data or credentials.

## Workstreams

| ID | Experience slice | Status | Latest critic | Largest gap | External evidence |
|---|---|---|---:|---|---|
| WS-00 | Synthetic all-screen visual scenario server | **passed** | **94/100; 0 critical/high; competitive** | None | Isolation: 12/12; viewport: 4/4; fixture integrity: 12/12; capture integrity: 7/7; Guide UI 63/63; Guide routes 11/11; settings, stats, guide provenance, login, and player-sheet addressability passed; browser smoke passed; 2,998/2,998 tests passed before final focused Guide verification; 90 clean screenshots; content-addressed gate complete |
| WS-01 | Visual foundations, typography, spacing, elevation, and motion | **in progress** | **Baseline 79/100; offline-banner slice passed 98/100** | Desktop library/search/stats remain mobile-width compositions with weak use of space | Offline banner geometry 5/5; offline 99/99; shell versions 122/122; 90-state scenario gate complete |
| WS-02 | App shell, routing, transitions, and status banners | queued | — | Awaiting baseline | Router and lifecycle suites |
| WS-03 | Login, first run, and account entry | queued | — | Awaiting baseline | Authentication/account suites |
| WS-04 | Library, shelves, queue, and book identity | queued | — | Awaiting baseline | Library/state suites |
| WS-05 | Search, source disclosure, upload, and import | queued | — | Awaiting baseline | Search/import suites |
| WS-06 | Player transport, chapters, progress, and resume | queued | — | Awaiting baseline | Playback and layout gates |
| WS-07 | Speed, sleep, bookmarks, chapter sheet, and start-over | queued | — | Awaiting baseline | Playback-control suites |
| WS-08 | Voices, narration status, and pronunciation repair | queued | — | Awaiting baseline | Voice/queue/repair suites |
| WS-09 | Offline preparation, downloads, storage, and degraded networks | queued | — | Awaiting baseline | Service-worker/offline suites |
| WS-10 | Settings, providers, accounts, Calibre, privacy, and diagnostics | queued | — | Awaiting baseline | Operator/security/Calibre suites |
| WS-11 | Book Guides | queued | — | Awaiting baseline | Guide certification/UI suites |
| WS-12 | Listening stats, queue, and progress insight | queued | — | Awaiting baseline | Stats/queue suites |
| WS-13 | Accessibility, focus, keyboard, dialogs, and touch targets | queued | — | Awaiting baseline | Accessibility audit must be added |
| WS-14 | Background playback, lock screen, handoff, and interruption recovery | queued | — | Awaiting baseline | Playback continuity gates |

## Known evidence limits

- Browser smoke is Chromium-based and is not proof from a physical iPhone or installed iOS PWA.
- Existing accessibility assertions are focused; the project does not yet have a comprehensive automated WCAG tree audit.
- Reference products may require credentials, payment, or unsupported hardware. Critics must record these limits and must not invent comparisons.
- Subjective competitiveness remains model-judged and is separate from executable completion evidence.

## Next work

1. Adapt desktop library, search, and Stats into intentional desktop compositions without weakening the strong mobile player.
2. Send only that gap to a fresh builder, then rerun focused and external evidence.
3. Continue WS-01 until a fresh whole-slice critic reaches at least 90/100 with no critical/high defect.

Contract receipt: validation has zero warnings and one honest mixed-grounding note; verification is bounded, deterministic, and resume-stable; the native score is 91/A. Standalone compilation succeeded with the preserved warning that artifact exposure is soft-enforced rather than a filesystem write sandbox.

Wave 1a stopped at the no-progress cap because Loopy v0.8 did not parse Claude's fenced JSON result, leaving the decomposed workstream list empty. The journal is preserved under run ID `xandrio-product-wave-01`. The role router now normalizes fenced structured results while preserving Claude's metered usage envelope; the corrected run uses a new ID.

Wave 1b (`xandrio-product-wave-01b`) decomposed successfully into the 15 workstreams above. Its WS-00 Sonnet builder created the isolated synthetic scenario environment but exceeded the configured 45-minute agent boundary. Loopy journaled the failed effect without accepting a builder result. An earlier independent run produced 71 screenshots, but the builder's later cleanup removed PNG covers while fixture metadata still referenced them. A clean external-gate run is now `actionable` with 0 fresh screenshots. The focused recovery LoopSpec validates with 0 warnings and one honest mixed-grounding note, verifies as bounded/deterministic/resume-stable, scores 91/A, and preserves the standalone soft artifact-contract warning.

Fresh Opus critic run `xandrio-ws00-wave-01` operated the artifact and returned 34/100, 5 critical/high defects, and `competitive: false`. It proved that `browser.newContext({width,height,...})` silently produced 1280×720 contexts: mobile PNGs were 3840×2160 only because DPR was 3, while desktop PNGs were 1280×720 rather than the declared 1280×800. It also recorded—but did not choose as the single current gap—the missing PNG clean-boot failure, seven semantic-state collisions, wrong search submit selector, absent stateful-sheet scenarios, blocked service-worker coverage, flat covers, and a documented guide flake. Critic usage: 35,516 tokens and $3.6446. A fresh Sonnet builder is now scoped only to the viewport gap.

The fresh Sonnet builder fixed only the viewport gap by nesting `viewport: {width,height}` in Playwright context options and adding an assertion that reads PNG IHDR dimensions after every capture. It also added a standalone viewport regression test. Independent execution passed 4/4 assertions: mobile reports 390×844 and renders 1170×2532 at DPR 3; desktop reports and renders 1280×800. Builder usage: 24,658 tokens and $1.0248. Loopy then paused at the explicit dollar cap. No scenario or Loopy process remained.

Fresh Opus critic run `xandrio-ws00-wave-02` returned 24/100, 4 critical/high defects, and `competitive: false`. Its single largest gap is clean reproducibility: `books.json` references four `.png` covers that do not exist, so dataset provisioning throws before any fresh screenshot. It independently confirmed the viewport fix and recorded additional gaps: collapsed search/settings/login states, an incomplete collision gate, leaked failure-path servers, partial network/environment isolation, flat covers, no stateful sheets, and blocked service-worker testing. Critic usage: 32,681 tokens and $3.7333. Fifteen parentless scenario servers from failed critic probes were terminated explicitly; the active builder was not touched.

The Wave 2 Sonnet builder changed all four fixture cover references to the existing JPEG assets and added a fast integrity test. Independent verification passed 12/12 fixture assertions. A clean `scenario:verify` then rendered 71 fresh screenshots and returned `actionable` only for seven semantic collisions: search loading/error/full equal empty; settings error/degraded/full equal loading; and login full equals offline. Builder usage: 8,859 tokens and $0.4720. Loopy paused at the explicit dollar cap after the builder.

Fresh Opus critic run `xandrio-ws00-wave-03` returned 58/100, 3 critical/high defects, and `competitive: false`. Its single largest gap is the gate exit contract: two independent 71-screenshot runs returned `actionable` with seven failures but process exit code 0. The critic independently reproduced the wrong search-clear selector, inert settings fault endpoint, login offline collision, partial collision coverage, incomplete network/environment isolation, missing sheets, and a nondeterministic guide state. Critic usage: 33,731 tokens and $4.2724, which crossed the $4 run cap before a builder could start. A separate validated 96/A, one-iteration builder LoopSpec is now applying only the gate-contract fix with a $1.50 cap.

The one-gap gate-contract builder completed under the explicit Codex/Terra fallback after Claude reached its session limit. `actionable` now preserves the single JSON judge envelope and exits with status 1 for normal shell and CI callers. Independent `npm run scenario:verify-exit` passed. The builder-only LoopSpec originally named a nonexistent `scenario:verify-exit-policy` command; Loopy v0.8 journaled that nonzero shell effect as done, so that receipt is rejected as proof. The input now names the real regression command. The authoritative full repository run outside the socket-restricted sandbox passed 2,996/2,996 tests across 148 suites.

The standalone Android lock-screen verifier had drifted behind Xandrio's continuous transport: its fixture never reported playback runway readiness and still expected the removed per-chapter `/api/audio-stream` source. The fixture now reports readiness, serves the current `/api/audio-continuous` transport with a chapter timeline, and asserts that one user-started media element crosses the chapter boundary without a source replacement. The focused gate passed with 210.7 ms first sound, 107.0 ms longest stall, 9.09 seconds of locked advance, one continuous request, and zero fallback requests. `verify:android-lockscreen` is now a required aggregate core gate.

Fresh Opus critic run `xandrio-ws00-wave-04b` returned 54/100, 3 critical/high defects, and `competitive: false`. Its single largest gap was that declared scenario states had no live state-specific proof; all five search states were one untouched page because the interaction clicked `#search-clear-btn`, and the enabled source set did not contain the prior `meridian` query. Critic usage: 29,203 tokens and $2.8415. The preceding `wave-04` journal is a failed receipt from the old 10-minute adapter timeout and contains no critic result.

The Wave 4b Sonnet builder exceeded the corrected 30-minute adapter boundary and returned no builder claim. Its surviving scoped changes were therefore accepted only after independent execution: search now targets `#search-btn`, uses a fixture query available through enabled sources, the provider stub filters search responses by query, and applicable cells assert visible/absent DOM signatures before writing screenshots. A clean `scenario:verify` rendered all 71 screenshots and reduced the actionable collision list from seven to four: settings error/degraded/full still equal loading, and login full still equals offline. Search empty/loading/skeleton/error/full no longer collide.

Fresh Opus critic run `xandrio-ws00-wave-05` returned 41/100, 4 critical/high defects, and `competitive: false`. Its single largest gap was a proven network-isolation breach: while `search:full` fetched a result cover, a real dataset-server child held an established TLS socket to the live Gutendex address because global `fetch()` bypassed the request-only guard. The critic also confirmed broad operator-environment inheritance. Critic usage: 29,406 tokens and $3.4958. Claude prefaced the fenced JSON with one sentence, so the old router stored it as a string and Loopy started a duplicate critic; the duplicate and the critic's exact orphaned scenario server were terminated, and the router now extracts a fenced result anywhere in the response.

The isolation repair now intercepts non-loopback `request()`, `get()`, and global `fetch()` paths, preserves method/body/target metadata through the local stub, and fails closed for unsupported schemes or a missing stub port. Dataset children now receive a fixed scenario-only environment instead of `process.env`; representative PPQ, OPDS, Standard Ebooks, web-push, and private-Codex credentials and flags are excluded. Independent `scenario:verify-isolation` passed 12/12 checks. The first Sonnet builder crossed its token cap after the fetch repair; a one-iteration Terra builder completed the residual environment allowlist. Loopy v0.8 again mislabeled its nonzero shell check as done because macOS injected harmless `__CF_USER_TEXT_ENCODING`; the regression now explicitly permits only that platform variable, and the independent rerun passes.

The first clean full scenario run after isolation repair stopped at 45/71 screenshots because `guide:loading` never satisfied its newly required `#guide-body .settings-hint` signature. The gate returned `actionable` and exit 1. This is not accepted as an isolation regression: the strict state contract is now correctly refusing to write an unproven guide state, and a fresh critic must select the next repair.

Fresh Opus critic run `xandrio-ws00-wave-06` returned 72/100, 2 critical/high defects, and `competitive: true`. Its single largest gap was the finite sweep: deterministic `guide:loading` and desktop `player:full` signature failures aborted the entire run and left stale screenshots looking current. It confirmed the 12/12 structural isolation result, correct mobile/desktop dimensions, real Xandrio rendering, custom-port support, and clean process teardown. Critic usage: 14,950 tokens and $1.8618.

The Wave 6 Sonnet builder restored only the safe runtime `PATH` needed for real `ffmpeg` child execution; omitting it from the new credential-free environment had caused genuine narration errors that cascaded into the guide/player signature failures. It also changed the screenshot sweep to attempt every applicable cell, collect all failures, and exit nonzero only after teardown, so a failed cell can no longer leave an apparently complete stale artifact tree. The builder paused at the $4 wave cap after using 48,761 tokens and $2.9998. Five named scratch diagnostic files were removed. Independent `scenario:verify` rendered all 71 screenshots and returned `actionable` only for the four known settings/login collisions.

Fresh Opus critic run `xandrio-ws00-wave-07` returned 72/100, 2 critical/high defects, and `competitive: true`. Its single largest gap was Settings state addressability: the closed-by-default Voice accordion kept loading, error, degraded, and full captures visually indistinguishable. The first Sonnet builder failed at the Claude harness boundary and made no change. A fresh budget-conscious Terra fallback added a real Voice-accordion interaction, state-specific endpoint/signature coverage, and mobile/desktop Settings variants. Independent verification then exposed a residual framing defect: the degraded engine-down card existed in the DOM but sat below the fixed desktop viewport, leaving the screenshot byte-identical to full.

A fresh scoped Terra builder fixed only that framing defect by scrolling the opened `#voice-list` into the capture viewport. Independent `scenario:verify-settings` now passes across loading, error, degraded, and full on both mobile and desktop. Independent `scenario:verify-isolation` still passes 12/12 after the required `PATH` allowlist adjustment. The complete external `scenario:verify` rendered all 74 screenshots and returned the auditable actionable fingerprint `42a50eca64b03926617f99c644259b3d86815c3e3e66700402e1eded3d851736` for one remaining collision only: `login:full` is byte-identical to `login:offline`.

Bounded Loopy run `xandrio-ws00-wave-08` failed before iteration 0 because the Claude Code harness exited before returning a critic result; its journal is preserved as a failed harness-start receipt and is not quality evidence. A fresh read-only GPT-5.6 Sol critic fallback then operated the running artifact and returned 84/100, 1 high defect, and `competitive: false`. It independently confirmed fixture integrity, correct viewport dimensions, the credential allowlist, 12/12 network isolation, custom-port routing, loopback-only sockets, clean termination, a fresh 74-shot sweep with zero failures, and an additional 43-cell desktop sweep with zero signature failures. Its single largest gap is that chapters/bookmarks, voice, speed, sleep, pronunciation, and audio-activity player overlays are not first-class matrix routes or captures, while current DOM checks may accept distinguishing elements outside the screenshot viewport. Direct mobile inspection proved this defect with degraded player content at vertical coordinates 1114–1176 against an 844px viewport. Reference comparison was limited to official public Audible, Apple Books, Libby, Spotify, and Speechify documentation; no signed-in, paid, borrowed, private, or native-only competitor flow was accessed.

A fresh scoped Terra builder added first-class chapters, bookmarks, voice/full and voice/degraded, speed, sleep, pronunciation, and audio-activity cells. Each uses the real product route and interaction, captures mobile and desktop, and requires every positive DOM signature to intersect the screenshot viewport before a PNG is accepted. Audio Activity starts the real offline-preparation action in an isolated environment per viewport so its short fixture cannot finish between captures. Independent `scenario:verify-player-sheets`, `scenario:verify-settings`, `scenario:verify-isolation` (12/12), and `scenario:verify-exit` all pass. The complete expanded gate rendered 90/90 screenshots with no capture failures and returned actionable fingerprint `2159704a0f78a93278984d8905a2738fec7d2a9a7fb3585e4f2ad2b306a93f18` for the sole remaining semantic collision: `login:full` is byte-identical to `login:offline`.

Fresh read-only GPT-5.6 Sol critic Wave 9 operated the expanded running artifact and returned 88/100, 1 high defect, and `competitive: false`. It independently reproduced the 90-shot gate and a fixed-port run, with `login:full` and `login:offline` both hashing to `c67dfd…`; confirmed all player-sheet evidence intersects mobile and desktop viewports; passed fixture, viewport, isolation, Settings, player-sheet, and exit regressions; and proved custom-port teardown. Its single largest gap is real product behavior: Xandrio returns behind the authentication gate before offline status initializes, so a disconnected reader sees an ordinary sign-in form with no offline explanation or unavailable-action feedback. Public comparison used official Audible, Apple Books, Libby, Spotify, and Speechify documentation only; no signed-in, purchased, native-only, private, or paywalled flow was claimed.

A fresh scoped Terra builder added auth-independent connectivity handling to the real login gate before the application can return from authenticated boot. Offline sign-in now shows an accessible polite status, disables and relabels the submit action, blocks credential submission defensively, and responds to online/offline transitions without changing authenticated offline playback initialization. It added focused initial-offline and transition coverage and updated the scenario signature to require the visible state. Independent evidence passed: 122 app-shell/version assertions, 2 login-offline browser tests, full browser smoke, and 12/12 isolation checks. The complete 90-shot WS-00 gate returned `complete` with zero failures and fingerprint `0eaa814d194cb4a855b1be4bced75c029c574f97bacc9030a4982af0c56a4a1b`.

Fresh read-only GPT-5.6 Sol critic Wave 10 rejected that executable success and returned 86/100, 1 high defect, and `competitive: false`. It operated a fresh 90-shot run, inspected every base and sheet state at mobile and desktop, tested large text and custom port 58439, and verified teardown and isolation. Its single systemic gap is insufficient state isolation and negative validation: the gate can certify rendered target states containing a stale Guide warning and clipped digest, a player error toast, playback-attempt warnings, and duplicate bookmarks created across shared variants. Current positive checks require only viewport intersection, not full containment or absence of unexpected banners/toasts and cross-variant mutations. Public comparison used accessible official Audible, Apple Books, Libby, Spotify, and Speechify pages only; no signed-in, purchased, private, or native-only competitor flow was claimed.

Fresh scoped Terra builders repaired the Wave 10 gap and its independently exposed residuals. Every player capture variant now receives a fresh single-dataset environment; bookmark evidence requires exactly one row; positive evidence must fit fully inside the viewport; and a default negative contract rejects stale-guide, error-toast, deployment, unexpected offline, audio-error, and playback-warning contamination, with narrow explicit exceptions only for the target error/offline cells. The ready-guide artifact now derives its source fingerprint and chapter-structure identity from the persisted synthetic book. Seeded listening positions now carry matching chapter-structure keys so the real Stats API does not correctly filter them into a false empty state. Focused capture-integrity (6/6), guide provenance (2/2), Stats, player-sheet, Settings, viewport, fixture, exit, and isolation (12/12) regressions pass. The full repository suite passed 2,998/2,998 tests across 149 suites.

Independent verification also found that the executable fingerprint ignored PNG bytes, so materially changed screenshots could retain the same receipt. A fresh scoped Luna builder changed the gate fingerprint to include every sorted relative screenshot path and SHA-256 content digest; its regression proves stable bytes remain stable and changed bytes at the same path change the fingerprint. The final complete 90-shot gate has zero failures and content-addressed fingerprint `7bf3274c6e0f46b4b2a9da8bd545550e57059cbd0df94c6f99d8241fa76b52b3`.

Fresh read-only GPT-5.6 Sol critic Wave 11 returned 89/100, 0 critical/high defects, and `competitive: true`, narrowly below the 90-point pass threshold. It independently reproduced a clean 90-shot gate and exact content fingerprint `8f26f084c66a9cc5453afe937617edee6efb8c1ba035edc6b4f93e161064ed24`; confirmed the ready Guide has no stale/debug provenance; found no error/playback warnings or duplicate bookmarks; passed all focused integrity, provenance, viewport, teardown, and isolation checks; and judged Xandrio's covered feature breadth competitive with accessible official reference documentation. Its single remaining gap is ordering in the large-text capture contract: full-containment validation runs before the 1.4× zoom, so the fresh Settings full large-text image can move the Voice cards entirely out of frame after the check while the gate remains green. The aggregate core gate also completed with fingerprint `ad89743f2018d281bb0e801f714e4f8f88125e56e6a63a5c90c7889e64097c6a`; limitations remain Node 26 versus production Node 24, Chromium rather than physical iOS, and no comprehensive WCAG-tree audit.

A fresh scoped Terra builder moved large-text scaling immediately after navigation, before route activation, interactions, scrolling, negative/duplicate checks, and viewport containment. The existing signature path now scrolls and validates evidence against the final 1.4× layout rather than the pre-zoom layout. A new focused regression proves that evidence fitting at normal scale but leaving the viewport after zoom is rejected. Independent capture-integrity (7/7), Settings, and viewport (4/4) checks pass. The final 90-shot external gate is complete with zero failures and content-addressed fingerprint `f746eb4044a36c37dd36de88682478f328843effbb7aff346f957ff764a0a023`.

Fresh read-only GPT-5.6 Sol critic Wave 12 independently reproduced a clean 90-frame run and passed every focused fixture, viewport, isolation, provenance, login, Settings, Stats, player-sheet, fingerprint, custom-port, and teardown check. It confirmed final-layout evidence remains in frame at 1.4× and found no stale state, duplicate bookmarks, unexpected warnings, private data, credentials, or live egress. It nevertheless returned 88/100, 0 critical/high defects, and `competitive: false` because the actual Guide error/degraded UI exposes `External destination` plus full generator and verifier SHA-256 provenance directly to readers. Its single gap is to replace those internal diagnostics with safe recovery copy while retaining operator-only diagnostics. Reference comparison used only accessible official Audible, Apple Books, Libby, Spotify, and Speechify material; signed-in, entitled, purchased, private, and native-only flows remained inaccessible.

A fresh scoped Terra builder replaced reader-facing Guide destination/model/digest diagnostics with concise capability-aware recovery. Administrators retain safe `Try again` and `Open settings` actions plus generic rights/cost disclosure; members receive an administrator-handoff message without management controls. Full generation diagnostics remain available only through the admin response path. Independent Guide UI (63/63), Guide routes (11/11), Guide service (21/21), app-shell versions (122/122), and browser smoke passed. A subsequent full gate exposed an intermittent but real `player:cold` fixture mismatch: the cold dataset contained no routed book, so the first-run notice could cover a missing-book error. Cold now means unacknowledged rather than empty and retains the synthetic library behind the operator notice; focused capture passed without contamination. The complete gate then returned 90/90 with zero failures and fingerprint `fd26e52382e98fc0d5595e55600cecdb2d3dc8a217d494d2dd358b138d4a56a5`.

The one agreed final fresh GPT-5.6 Sol cutoff critic passed WS-00 at 94/100 with 0 critical/high defects and `competitive: true`. It operated the running product, visually inspected every base state and first-class player/activity/login surface at mobile, desktop, normal text, and 1.4× text, and independently produced 90/90 fresh frames with content fingerprint `442ff5d11ce21f73d00bb3c73d2485e7a42a325acce75ce381d9f5fb82d1d317`. It confirmed reader-safe Guide recovery, no cold-player error, no stale/debug provenance, no unexpected warnings/toasts, one bookmark row, final-frame containment, custom-port teardown, loopback-only requests, synthetic fixture identities, and credential/environment isolation. The critic judged Xandrio preferable for self-hosted bring-your-own-book study workflows against accessible official public reference material. Access limits: Chromium rather than physical devices, 1.4× CSS zoom rather than OS Dynamic Type, service workers blocked in scenario captures, and no signed-in, entitled, purchased, private, paywalled, or native-only competitor flows.

WS-01 opened with two independent running-product assessments. The design-director pass returned 79/100 with one P1; it judged the mobile player strong and authored, but found the responsive app shell uncompetitive. The evidence pass returned 88/100 and independently measured a Settings helper-text contrast failure, a 26×26 Continue Listening dismiss target, and three fragile empty image sources. The persisted critique is under `.impeccable/critique/`. The Gauntlet selected the highest-severity visible slice first: the fixed global offline banner obscured primary navigation.

A fresh Terra builder made the app reserve the banner's measured height and added a rendered geometry regression. The first fresh Sol critic rejected the mobile-only repair at 76/100 because desktop Library, Player, and Settings headers remained covered. The builder corrected the scope to every viewport and expanded the regression. A second fresh Sol critic then passed the bounded repair at 98/100 with zero P0/P1 defects after nine live route/viewport checks: no overlap, overflow, clipping, desktop drift, or reconnect gap. Independent focused evidence passed 5/5 geometry checks, 99/99 offline tests, and 122/122 shell-version tests. The complete 90-state scenario gate returned `complete` with zero failures and content-addressed fingerprint `74d84afff949a8cacc3d2d0e6926e2f3056b460180d9e01f37c43fb487f14cad`. The next single WS-01 gap is intentional desktop composition for Library, Search, and Stats.

<!-- GAUNTLET_EXTERNAL_START -->
## Latest external gate receipt

- Run: `2026-08-22T04-46-29-918Z-678e7170`
- Commit: `3469d08cffdf716ff0d3ef7f0c74df15dbc781b0`
- Profile: `core`
- Result: **passed**
- Environment: Node v26.7.0 on darwin
- Private evidence: `logs/gauntlet/2026-08-22T04-46-29-918Z-678e7170`

| Gate | Status | Duration | stdout SHA-256 |
|---|---:|---:|---|
| diff-check | passed | 0.0s | `e3b0c44298fc` |
| unit-regression | passed | 87.9s | `f48f07ae4dfb` |
| browser-pwa | passed | 28.5s | `e8f89f731939` |
| audio-policy | passed | 1.0s | `661cc459f7e6` |
| playback-performance | passed | 21.4s | `7ff0d03adcbf` |
| android-lockscreen | passed | 6.8s | `869ed7dcea8c` |
| player-layout | passed | 11.2s | `20831c5026c0` |
| release-audit | passed | 0.8s | `37b78ab386be` |
| docker-context | passed | 5.6s | `33d117d5bfd3` |

Limitations: Executable gates do not establish subjective reference-product parity. Chromium browser smoke is not a physical iOS/PWA test. A comprehensive automated WCAG tree audit is not yet present.
<!-- GAUNTLET_EXTERNAL_END -->
